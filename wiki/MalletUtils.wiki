#Utilities that MALLET offers.

= Introduction =

Utilities for lexing, logging and Math functions

= contents = 
<wiki:toc max_depth="2" />

 ----------------------------------------------------------------------------------------
 ----------------------------------------------------------------------------------------

= Interfaces =

 ----------------------------------------------------------------------------------------
 ----------------------------------------------------------------------------------------

= Classes =
== Property-List==
Property Lists are tabular data structure containing zero or more entries, each entry is associated with a key, which is called value or property. Important property of the property list is, any update to it is a destructive(alters the property list rather than making a new one). A list contains an even number of elements that are alternating names (sometimes called indicators or keys) and values (sometimes called properties). When there is more than one name and value pair with the identical name in a property list, the first such pair determines the property.

Mallet Property List works with Object and Numerical (Double) objects.

public class PropertyList
extends java.lang.Object
implements [http://mallet.cs.umass.edu/api/serialized-form.html#cc.mallet.util.PropertyList java.io.Serializable]

===Nested Classes supported===
 * PropertyList.Iterator           
 * PropertyList.NumericIterator
 * PropertyList.ObjectIterator
           <To be done>
===Property-List-Constructors===
 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html protected PropertyList()]
 Does not allow zero parameter constructor, throws [http://java.sun.com/j2se/1.4.2/docs/api/java/lang/IllegalArgumentException.html IllegalArgumentException]
           
 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html protected PropertyList(java.lang.String key, PropertyList rest)]
 ====Parameters====
 key - Key Indicator to be used.
 rest - Reference to PropertyList object.

 Since, both the constructors are protected Property List objects are instantiated using add methods (explained below).
  
[MalletUtils#Contents Contents]

===Property-List-Methods===
 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html#add(java.lang.String,%20double,%20cc.mallet.util.PropertyList) static PropertyList add(java.lang.String key, double value, PropertyList rest)]
 Creates a property list entry and adds its at the front of the property list. this methods creates numerical entry and adds it to the plist.
 ====Parameters====
 # key - Indicator of the entry. type - String.
 # value - Value of the entry. type - double.
 # rest - Reference of the existing plist to which the entry needs to be added or null if the first entry needs to be created.
 ====Return Value====
 PropertyList - Updated plist or newly created plist

 Code snippet for instantiating plist
{{{
PropertyList pl = null;
pl = pl.add ("key", 1.0, pl);
}}}

 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html#add(java.lang.String,%20java.lang.Object,%20cc.mallet.util.PropertyList) static PropertyList add(java.lang.String key, java.lang.Object value, PropertyList rest)]
 Creates a property list entry and adds its at the front of the property list. this methods creates numerical entry and adds it to the plist.

 ====Parameters====
 # key - Indicator of the entry. type - String.
 # value - Value of the entry. type - Object.
 # azzcxrest - Reference of the existing plist to which the entry needs to be added or null if the first entry needs to be created.

 ====Return Value====
 # PropertyList - Updated plist or newly created plist

 Code snippet for instantiating plist
{{{
PropertyList pl = null;
String str = "value";
pl = pl.add ("key", (Object)str, pl);
}}}

 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html#add(java.lang.String,%20java.lang.String,%20cc.mallet.util.PropertyList) static PropertyList add(java.lang.String key, java.lang.String value, PropertyList rest)]
 Creates a property list entry and adds its at the front of the property list. this methods creates numerical entry and adds it to the plist.

====Parameters====
 # key - Indicator of the entry. type - String.
 # value - Value of the entry. type - String.
 # rest - Reference of the existing plist to which the entry needs to be added or null if the first entry needs to be created.

====Return Value====
 # PropertyList - Updated plist or newly created plist

 Code snippet for instantiating plist
{{{
PropertyList pl = null;
pl = pl.add ("key", "value", pl);
}}}
 * [http://mallet.cs.umass.edu/api/cc/mallet/util/PropertyList.html#remove(java.lang.String,%20cc.mallet.util.PropertyList) public static PropertyList remove (String key, PropertyList rest)]
 The name remove is really an irony here!!! All it does is create a new entry with with value set to null. Strange isn't it? I was astonished too, but look at the definition of plist it makes sense.

====Parameters====
 # key - Indicator of the entry that needs to be removed. type - String.
 # rest - Reference of the plist from which the entry needs to be deleted.

====Return Value====
 # PropertyList - Updated plist or newly created plist


[MalletUtils#Contents Contents]

 ----------------------------------------------------------------------------------------
 ----------------------------------------------------------------------------------------